{
  "name": "automation",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "dependencies": {
    "@sparticuz/chromium": "^138.0.2",
    "axios": "^1.14.0",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "form-data": "^4.0.5",
    "node-cron": "^4.2.1",
    "puppeteer": "^24.22.3",
    "puppeteer-core": "^24.22.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/form-data": "^2.2.1",
    "@types/node": "^24.12.2",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "run": "node dist/index.js",
    "build": "tsc",
    "start": "npm run build && npm run run",
    "docker": "docker system prune && docker build -t automation . &&  docker run -p 3000:3000 automation",
    "docker-push": "docker system prune -f && docker build -t automation:${npm_config_tag} . && docker tag automation:${npm_config_tag} vloglife643/automation:${npm_config_tag} && docker push vloglife643/automation:${npm_config_tag}"
  }
}
